# siphonophore lab notebook

Claim-anchored experiments building `siphonophore-core` bottom-up (DESIGN.md SS6: define core
contracts, implement minimum SDK, build a tiny reference harness proving the central invariant,
discover which abstractions were wrong, refine, expand). Each numbered pair is one experiment:
hypothesis (with an explicit null), method, procedure, real results, honest analysis, conclusion.
Modeled on `~/dev/experiments/substrate-prototype/lab/` and `~/dev/experiments/e-forge/lab/`.

Every script is self-contained and imports nothing beyond the Python standard library — no
dependency on any other file in this repo, any other repo, any installed package, or this
project's own git history (DESIGN.md SS0; see HISTORY.md's account of the one time that principle
was violated, and why the fix was deletion rather than a cleaner import).

## Index

| # | Date | Title | Headline finding |
|---|---|---|---|
| 001 | 2026-08-25 | [Gate blocks unmediated effects](001-gate-blocks-unmediated-effects.md) | Hypothesis **supported** (6/6 predicates). Minimal `Intent -> Gate.submit() -> Decision -> Executor.execute()` pipeline: a mediated write's on-disk content is confirmed by reading the file back; two independent Decision-forgery strategies (made-up token, attacker's own HMAC secret) are both refused by `Executor.execute()`'s own verification, with the target file confirmed absent. Named an open trust boundary: payload fields are not yet bound into the token, only `intent_id`/`principal_id`/`kind`/`permitted` are. |
| 002 | 2026-08-25 | [Delegation through the same gate](002-delegation-through-the-same-gate.md) | Hypothesis **supported** (3/3 predicates, 11/11 checks). `delegate` added as a second `Intent.kind`; `Executor.execute()` handles it by constructing a *new* Intent for the sub-agent's own effect and submitting it through the same Gate — no shortcut. Sub-agent's effect confirmed on disk, attributed to its own `principal_id`. Forged delegate Decision refused. `kind` was bound into the HMAC from the first line this time; both a token-reuse relabel and an in-place `kind` mutation on a genuine Decision correctly failed `Gate.verify()` and were refused — the exact gap HISTORY.md describes finding only after the fact, this time built in from the start. |
| 003 | 2026-08-25 | [Execution class selection](003-execution-class-selection.md) | Hypothesis **supported** (4/4 predicates, 11/11 checks). `consequence` on `Intent`, `execution_class` (`same_process`\|`separate_process`) on `Decision`. Real, distinct PIDs prove the class changes *where* the effect runs: same_process effect's pid == Executor's own pid (79144); separate_process effect's pid (79145) self-reported by a real spawned `subprocess.run` child via its own stdout, not assumed by the parent. `execution_class` bound into the HMAC from the start; a downgrade-replay of a genuine `separate_process` Decision to `same_process` correctly failed verification and was refused — second independent confirmation (after 002's `kind`) of the DESIGN.md SS2 standing rule. |
| 004 | 2026-08-25 | [uid+cgroup as a real third execution class](004-uid-cgroup-execution-class.md) | Hypothesis **supported** (6/6 predicates, 16/16 checks), actually run as root on colima (Ubuntu 24.04.4, real Linux). Refusal path confirmed first on both macOS (non-Linux) and colima-without-sudo (non-root). Real run: `uid_cgroup` class provisioned a genuinely distinct ephemeral uid (62001, vs. broker's own uid 0), confirmed by the root parent reading `/proc/<pid>/status` (kernel ground truth) and agreeing with the child's self-report; confirmed a real member of its own cgroup (`cgroup.procs`) while still alive; cleanly released (user + cgroup both gone) afterward. Forged Decision and downgrade-replay (to `same_process`) both refused. uid/cgroup provisioning built entirely fresh, no reuse of the deleted v1 `identity.py`. Two real, honestly-documented gaps found on the first attempt: a scratch-directory permission failure (fixed) and an exception-unsafe cleanup path that leaked a real user+cgroup on that same failure (named, not fixed — see write-up). |
| 005 | 2026-08-25 | [Delegation with uid+cgroup, trusted only after check-in](005-delegation-with-uid-cgroup.md) | Hypothesis **supported** (4/4 predicates, 18/18 checks), real root on colima. Combines 002 (delegation through the same gate) with 004 (uid+cgroup): a delegated sub-agent runs under its own provisioned uid, but is trusted only after a real check-in — a nonce (inherited pipe fd, never argv) plus `SO_PEERCRED` both verified. Two independent kernel sources (`SO_PEERCRED`, `/proc/<pid>/status`) agreed with each other and the child's self-report. Impersonation (wrong nonce) and a never-checking-in sub-agent (timeout) both correctly refused, with clean release on *both* failure paths — fixing the exact gap 004's write-up named but left open (release logic moved into one outer `finally` wrapping the whole provision-through-dispatch sequence). Forged Decision refused with zero provisioning side effects. All 18 checks passed on the first real attempt — no methodology slip this time, stated honestly rather than assumed unremarkable. |
| 006 | 2026-08-25 | [Shared check-in listener under real concurrency](006-shared-checkin-listener.md) | Hypothesis **supported** (3/3 predicates, 18/18 checks), real root on colima. Unlike 005 (a dedicated socket per delegation, which can't be confused by construction), a `CheckinRegistry` + `SharedCheckinListener` serve many pending registrations on ONE shared socket, routed strictly by nonce. 12 real concurrent delegations across 3 rounds showed zero cross-attribution by three independent measures; a real cross-identity attack (delegation B's real provisioned uid presenting delegation A's real nonce) was correctly rejected without consuming or corrupting A's own registration — A's genuine check-in still succeeded afterward and its result recorded the rejected attempt. Getting real concurrency to run cleanly took four escalating, honestly-documented fixes for a Python/OS-level subprocess fd-race (`preexec_fn` under threads, then serialized creation, then serialized reaping, then finally restructuring into spawn-sequential / await-concurrent / reap-sequential phases) unrelated to the mediation logic itself. A secondary, non-security-relevant gap was found by checking the host directly rather than trusting the script's cleanup claims: crashed debugging runs left scratch tempfiles behind (provisioned uids/cgroups were still cleanly released on every crash, confirmed directly). |
| 007 | 2026-08-25 | [Belnap contradiction and unreported-activity](007-belnap-reconciliation.md) | Hypothesis **supported** (15/15 checks), real root on colima. First real code implementing DESIGN.md §3's four-valued (claimed, observed) → Belnap comparison (`reconcile()`/`reconcile_path()`). 001-006 only ever produced the T/T corroborated case; this experiment constructs real T/F and F/T cases from one real delegated sub-agent under its own provisioned uid: a genuine active lie (self-report claims specific content for a file whose real content, read independently by the root parent, is different — not merely a missing file) correctly classifies as `contradiction`; a genuine undisclosed extra write (discovered only because ground truth scans the whole output directory rather than just the claimed paths) correctly classifies as `unreported_activity`; T/T and F/F re-derived alongside them. Self-report (child's own inherited stdout fd) and ground truth (root's independent directory scan, collected only after independently confirming the child exited) never touch each other. All checks passed on the first real attempt. |
| 008 | 2026-08-25 | [Execution provenance](008-execution-provenance.md) | Hypothesis **supported** (9/9 checks), portable. First implementation of DESIGN.md §8's execution-provenance requirement: `Decision` gains `artifact_digest`, bound into the HMAC alongside `kind`/`execution_class`. Two distinct attacks tested, not one: a genuine `Decision` presented at execution time with *different code* than what was authorized is caught by an independent re-hash inside `Executor.execute()` — invisible to `Gate.verify()` alone, since the `Decision` itself was never touched; a `Decision` with `artifact_digest` swapped to a different real digest (not garbage) fails `Gate.verify()`, the same binding mechanism already proven for `kind`/`execution_class`. Kept deliberately free of uid/cgroup complexity — provenance and OS-level identity are orthogonal, tested separately on purpose. |

## Conventions

- **Numbering**: zero-padded three digits, append-only. Once assigned, an ID does not change.
- **Repo state**: every experiment records the siphonophore commit SHA at run time.
- **Environment**: every experiment states whether it's portable (any host, no root) or requires
  real root on real Linux (colima) — and, for the latter, records that it was actually run there,
  not assumed portable from code review alone (HISTORY.md's own lesson from v1).
- **Hypothesis discipline**: every experiment names a falsifiable claim with specific, checkable
  predicates and an explicit null. "It works end-to-end" is not a hypothesis.
- **Analysis is honest**: methodology slips, gaps found while writing, and partial confirmations
  get written up the same as clean wins — see 001's Analysis for an example (a trust-boundary gap
  named, not silently patched, on the same day it was found).
- **Out-of-scope**: pure engineering work (refactors, dependency bumps — there are none, by
  design) belongs in commit messages, not here.

## Template

[TEMPLATE.md](TEMPLATE.md). Copy, fill in, commit.
